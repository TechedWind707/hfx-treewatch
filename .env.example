# Copy to .env.local (Next.js) and .env (Prisma CLI) — both are gitignored.

# Anthropic API key used by the scoring endpoint.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the scoring model.
ANTHROPIC_MODEL=claude-sonnet-4-5

# Prisma / SQLite
DATABASE_URL="file:./dev.db"
